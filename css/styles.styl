@require 'base';
@require 'topbar';

.Wrapper {
    transition: all 0.3s ease;
    width: 100%;
    top: 0;
    left: 0;
    display: inline-block;
    overflow: hidden;
}

    .Wrapper-behind {
        position: relative;
        transform: scale(0.8) translateY(-5%);
    }
